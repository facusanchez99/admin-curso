import { Conversor2Pipe } from './conversor2.pipe';

describe('Conversor2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Conversor2Pipe();
    expect(pipe).toBeTruthy();
  });
});
