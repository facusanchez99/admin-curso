import { Conversor3Pipe } from './conversor3.pipe';

describe('Conversor3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Conversor3Pipe();
    expect(pipe).toBeTruthy();
  });
});
